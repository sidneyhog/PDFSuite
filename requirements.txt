pypdf>=4.0,<6.0

# OCR (modulo de verificacao de folhas por imagem) - motor 100% local, sem
# nuvem e sem instalador externo. Puxa onnxruntime + opencv + numpy (~100 MB).
# Se nao estiver instalado, o modulo de OCR se oferece para instalar sozinho
# na primeira vez; nao afeta os outros modulos do PDFSuite.
rapidocr-onnxruntime>=1.3,<2.0
